import React from 'react';
import { Button } from '@mui/material';

const MemeUpload = () => {
  return (
    <>
      <Button variant='contained'>Save Meme</Button>
    </>
  );
};

export default MemeUpload;
